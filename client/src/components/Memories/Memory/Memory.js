import React from 'react';
import useStyles from './styles';

function Memory() {
    const classes = useStyles();
    return (
        <div>
            MEMORY
        </div>
    )
}

export default Memory
